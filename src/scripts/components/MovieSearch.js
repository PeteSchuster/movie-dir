'use strict';

var React = require('react/addons');

require('styles/MovieSearch.scss');

var MovieSearch = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for MovieSearch</p>
        </div>
      );
  }
});

module.exports = MovieSearch; 

