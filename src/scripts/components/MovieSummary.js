'use strict';

var React = require('react/addons');

require('styles/MovieSummary.scss');

var MovieSummary = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for MovieSummary</p>
        </div>
      );
  }
});

module.exports = MovieSummary; 

