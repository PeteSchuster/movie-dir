'use strict';

var React = require('react/addons');

require('styles/MovieBrowse.scss');

var MovieBrowse = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for MovieBrowse</p>
        </div>
      );
  }
});

module.exports = MovieBrowse; 

