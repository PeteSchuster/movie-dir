'use strict';

var React = require('react/addons');

require('styles/MovieList.scss');

var MovieList = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for MovieList</p>
        </div>
      );
  }
});

module.exports = MovieList; 

